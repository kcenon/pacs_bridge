# =============================================================================
# benchmarks/CMakeLists.txt
# Performance benchmark configuration for PACS Bridge
# =============================================================================
#
# Benchmarks are disabled by default. Enable with:
#   cmake -B build -DBRIDGE_BUILD_BENCHMARKS=ON
#
# =============================================================================

# Placeholder for future benchmark targets
# Benchmarks will measure:
# - HL7 parsing throughput
# - Zero-copy parser performance
# - Message routing latency
# - Cache lookup performance
# - Connection pool efficiency

message(STATUS "Benchmarks directory configured (no targets yet)")
